# DEPRECATED: phpvm now strictly uses the nvm-windows symlink model.
# You can safely remove the `. phpvm.profile.ps1` import from your main User `$PROFILE`. 

Write-Host "Warning: phpvm.profile.ps1 is deprecated. PHPVM now sets its path dynamically at the Windows registry level." -ForegroundColor Yellow
